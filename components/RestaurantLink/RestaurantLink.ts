import styled from '@emotion/styled';

export const RestaurantLink = styled.a({
  fontSize: 13,
  color: 'blue',
  marginTop: 8,
  marginBottom: 16,
  alignSelf: 'start',
  ':hover': {
    textDecoration: 'underline',
  },
});
